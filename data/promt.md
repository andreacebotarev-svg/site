# Промпт для конвертации фото учебных материалов в JSON

Используйте этот промпт с ChatGPT-4 Vision, Claude 3, или другими AI моделями с поддержкой изображений для автоматической конвертации страниц учебников в структурированный JSON формат для Palace Engine.

---

## Промпт для AI

```
# Роль и задача
Ты - эксперт по извлечению структурированных данных из изображений учебных материалов. Твоя задача - проанализировать фотографию учебной страницы и преобразовать её содержимое в строго определённый JSON формат.

# Входные данные
Ты получишь фотографию страницы учебного пособия, которая может содержать:
- Текст для чтения (reading passages)
- Словарь (vocabulary) с английскими словами, транскрипцией, переводом на русский и примерами
- Фразы (phrases)
- Грамматические правила (grammar rules)
- Упражнения и вопросы (quiz questions)

# Структура выходного JSON

Создай JSON файл со следующей ТОЧНОЙ структурой:

```json
{
  "title": "string",
  "subtitle": "string",
  "meta": {
    "level": "string",
    "duration": number,
    "topic": "string"
  },
  
  "content": {
    "reading": [
      {
        "type": "paragraph",
        "text": "string"
      }
    ]
  },
  
  "vocabulary": {
    "words": [
      {
        "en": "string",
        "transcription": "string",
        "ru": "string",
        "example": "string",
        "part_of_speech": "string"
      }
    ],
    "phrases": [
      {
        "en": "string",
        "ru": "string"
      }
    ]
  },
  
  "grammar": {
    "title": "string",
    "explanation": "string",
    "rules": [
      {
        "rule": "string",
        "examples": ["string"]
      }
    ],
    "examples": {
      "affirmative": ["string"],
      "negative": ["string"],
      "questions": ["string"]
    }
  },
  
  "quiz": [
    {
      "question": "string",
      "options": ["string"],
      "correct": number,
      "correct_alt": number,
      "explanation": "string"
    }
  ]
}
```

## Описание полей

### Основные метаданные
- **title**: Главный заголовок урока
- **subtitle**: Подзаголовок (если есть, иначе пропустить)
- **meta.level**: Уровень сложности (A1, A2, B1, B2, C1, C2)
- **meta.duration**: Предполагаемая длительность урока в минутах
- **meta.topic**: Основная тема (например: "Education & Health", "Travel", "Technology")

### Секция content
- **reading**: Массив параграфов текста для чтения
  - `type`: всегда "paragraph"
  - `text`: полный текст параграфа

### Секция vocabulary
- **words**: Массив словарных единиц
  - `en`: английское слово
  - `transcription`: фонетическая транскрипция в формате [ˈdaɪət]
  - `ru`: русский перевод
  - `example`: пример использования на английском
  - `part_of_speech`: часть речи (noun, verb, adjective, adverb, preposition, conjunction, pronoun)

- **phrases**: Массив фраз и выражений
  - `en`: английская фраза
  - `ru`: русский перевод

### Секция grammar
- **title**: Название грамматической темы
- **explanation**: Общее объяснение правила
- **rules**: Массив правил с примерами
- **examples**: Примеры предложений (affirmative, negative, questions)

### Секция quiz
- **question**: Текст вопроса
- **options**: Массив вариантов ответа
- **correct**: Индекс правильного ответа (начиная с 0)
- **correct_alt**: Индекс альтернативного правильного ответа (если есть)
- **explanation**: Объяснение правильного ответа

# Критически важные инструкции

## 1. OCR и распознавание текста
- Извлеки ВСЕ видимые тексты с изображения
- Сохраняй орфографию и пунктуацию точно как в оригинале
- Если текст нечитаем или размыт, используй `null` вместо догадок
- Распознавай специальные символы: фонетические символы [ˈ ə ɪ ː], математические знаки
- Обрабатывай многострочные элементы (таблицы, списки)

## 2. Структурирование контента
- **Reading**: Каждый абзац - отдельный объект с `type: "paragraph"`
- **Vocabulary words**: Обязательно включай все поля: en, transcription, ru, example, part_of_speech
- **Phrases**: Только пары en-ru без дополнительных полей
- **Grammar**: Разделяй на title, explanation, rules с примерами
- **Quiz**: Нумерация правильных ответов начинается с 0 (первый вариант = 0)

## 3. Транскрипция
- Сохраняй транскрипцию в ТОЧНОСТИ как в оригинале
- Всегда используй квадратные скобки: `[ˈdaɪət]`
- Не изменяй фонетические символы
- Стандартные символы IPA: ˈ (ударение), ː (долгота), ə (шва), ɪ, æ, ʌ, ɜː, θ, ð, ʃ, ʒ, ŋ

## 4. Части речи (part_of_speech)
Используй ТОЛЬКО эти значения:
- `noun` (существительное)
- `verb` (глагол)
- `adjective` (прилагательное)
- `adverb` (наречие)
- `preposition` (предлог)
- `conjunction` (союз)
- `pronoun` (местоимение)

## 5. Обработка отсутствующих секций
⚠️ **ВАЖНО**: Если на фото НЕТ какой-то секции (например, grammar или quiz), **НЕ ВКЛЮЧАЙ её в JSON**.

Включай только те секции, которые фактически присутствуют на изображении:
- Есть текст для чтения → включай `content`
- Есть словарь → включай `vocabulary`
- Есть грамматика → включай `grammar`
- Есть вопросы → включай `quiz`

Пустые массивы `[]` допустимы только если секция есть, но пустая.

## 6. Индексация в quiz
- `correct: 0` → первый вариант
- `correct: 1` → второй вариант
- `correct: 2` → третий вариант
- `correct: 3` → четвёртый вариант

Используй `correct_alt` ТОЛЬКО если действительно есть два правильных ответа.

# Валидация и проверка качества

Перед выводом результата проверь:

✅ JSON синтаксически корректен (кавычки, запятые, скобки)
✅ Все тексты извлечены полностью без сокращений
✅ Индексы `correct` соответствуют реальным позициям вариантов ответов
✅ Транскрипция в квадратных скобках `[...]`
✅ `part_of_speech` использует только разрешённые значения
✅ Отсутствующие секции не включены в JSON

# Формат вывода

Выведи **ТОЛЬКО валидный JSON** без:
- Дополнительных комментариев
- Объяснений
- Markdown форматирования (```)
- Текста до или после JSON

Просто чистый JSON, который можно сразу сохранить в файл.
```

---

## Инструкция по использованию

### 1. Подготовка изображения
- Сфотографируйте страницу учебника в хорошем освещении
- Убедитесь, что текст читаем и не размыт
- Формат: JPG, PNG (до 20MB для ChatGPT)

### 2. Загрузка в AI модель

#### ChatGPT-4 Vision / GPT-4o
1. Откройте chat.openai.com
2. Нажмите 📎 (скрепка) и загрузите изображение
3. Скопируйте промпт выше и вставьте в чат
4. Дождитесь результата

#### Claude 3 Opus/Sonnet
1. Откройте claude.ai
2. Загрузите изображение через интерфейс
3. Вставьте промпт
4. Получите JSON

### 3. Сохранение результата
1. Скопируйте полученный JSON
2. Проверьте валидность на [jsonlint.com](https://jsonlint.com)
3. Сохраните в папку `data/lessons/` с именем `lesson_XX.json`

### 4. Проверка качества
- Сравните JSON с оригиналом изображения
- Проверьте транскрипцию слов
- Убедитесь, что индексы `correct` правильные
- Проверьте, что все секции корректно распознаны

---

## Примеры

### Минимальный пример (только reading)
```json
{
  "title": "Daily Routine",
  "meta": {
    "level": "A2",
    "duration": 30,
    "topic": "Daily Life"
  },
  "content": {
    "reading": [
      {
        "type": "paragraph",
        "text": "Every morning I wake up at 7 o'clock..."
      }
    ]
  }
}
```

### Полный пример
См. файл `lesson_01.json` в папке `data/lessons/` для примера полной структуры со всеми секциями.

---

## Частые проблемы и решения

### Проблема: AI добавляет секции, которых нет на фото
**Решение**: В промпте явно указано: "Если на фото НЕТ какой-то секции, НЕ ВКЛЮЧАЙ её". Напомните модели об этом.

### Проблема: Неправильные индексы в quiz
**Решение**: Проверьте, что нумерация начинается с 0. Первый вариант = 0, второй = 1, и т.д.

### Проблема: Транскрипция без квадратных скобок
**Решение**: Вручную добавьте квадратные скобки: `"transcription": "[weɪt]"`

### Проблема: AI выдает объяснения вместо JSON
**Решение**: Добавьте в конец промпта: "Выведи ТОЛЬКО JSON, без текста до и после."

---

## Технические детали

### Поддерживаемые AI модели
- ✅ ChatGPT-4 Vision (GPT-4o) - лучшее качество OCR
- ✅ Claude 3 Opus - хорошо с фонетическими символами
- ✅ Claude 3 Sonnet - баланс скорости и качества
- ✅ Gemini Pro Vision - альтернатива
- ⚠️ GPT-4 Turbo with Vision - может пропускать детали

### Ограничения
- Максимальный размер изображения: 20MB (ChatGPT), 10MB (Claude)
- Сложные таблицы могут требовать ручной проверки
- Рукописный текст распознается хуже печатного
- Мелкий шрифт (< 10pt) может быть нечитаем

---

## Связанные файлы

- `data/lessons/` - папка с готовыми JSON уроками
- `README.md` - общая документация проекта
- `schema.json` - JSON Schema для валидации (если есть)

---

**Последнее обновление**: December 2024  
**Версия промпта**: 2.0