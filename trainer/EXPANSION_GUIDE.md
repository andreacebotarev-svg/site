# 📝 Руководство по расширению проекта

> Подробная инструкция по добавлению новых уроков, слов и функционала

## 📋 Содержание

1. [Добавление нового урока](#добавление-нового-урока)
2. [Структура JSON урока](#структура-json-урока)
3. [Добавление слов в существующий урок](#добавление-слов-в-существующий-урок)
4. [Изменение дизайна](#изменение-дизайна)
5. [Добавление новой механики](#добавление-новой-механики)
6. [Тестирование](#тестирование)

---

## 📚 Добавление нового урока

### Шаг 1: Создайте JSON файл

Создайте новый файл в папке `data/trainer/` с именем `lesson_XX.json`, где XX - номер урока (например, `lesson_05.json`).

### Шаг 2: Заполните структуру урока

```json
{
  "id": 5,
  "title": "Урок 5: Ваше название",
  "emoji": "🎯",
  "rule": "Правило чтения",
  "description": "Краткое описание урока",
  "phonemesSet": ["s", "h"],
  "wordCount": 4,
  "estimatedTime": 5,
  "words": [
    {
      "word": "ship",
      "phonemes": ["sh", "i", "p"],
      "translation": "корабль",
      "transcription": "[ʃɪp]",
      "emoji": "🚢",
      "imageUrl": null
    }
  ]
}
```

### Шаг 3: Обновите список уроков в коде

Откройте `trainer/index.html` и найдите функцию `getAvailableLessons()`:

```javascript
async getAvailableLessons() {
    // Добавьте новый номер урока в массив
    const lessonIds = [1, 2, 3, 4, 5];  // ← Добавьте 5
    
    const lessons = [];
    for (const id of lessonIds) {
        try {
            const lesson = await this.load(id);
            lessons.push(lesson);
        } catch (error) {
            console.warn(`Lesson ${id} not available`);
        }
    }
    
    return lessons;
}
```

### Шаг 4: Закоммитьте изменения

```bash
git add data/trainer/lesson_05.json
git add trainer/index.html
git commit -m "Add lesson 5: [название]"
git push origin main
```

### Шаг 5: Проверьте результат

Подождите 2-3 минуты для деплоя GitHub Pages, затем откройте:
- https://eng-tutor.ru/trainer/

---

## 📦 Структура JSON урока

### Полная схема

```json
{
  "id": number,              // Уникальный номер урока (1, 2, 3...)
  "title": string,           // Название урока ("Урок 1: Буква E")
  "emoji": string,           // Emoji для карточки урока ("🔟")
  "rule": string,            // Правило чтения ("Буква E читается как [e]")
  "description": string,     // Описание урока
  "phonemesSet": string[],   // Набор фонем для урока (["t", "e", "n"])
  "wordCount": number,       // Количество слов (4)
  "estimatedTime": number,   // Примерное время в минутах (5)
  "words": Word[]            // Массив слов (см. ниже)
}
```

### Структура объекта Word

```json
{
  "word": string,           // Слово на английском ("ten")
  "phonemes": string[],     // Массив фонем (["t", "e", "n"])
  "translation": string,    // Перевод на русский ("десять")
  "transcription": string,  // Транскрипция ("[ten]")
  "emoji": string,          // Emoji для слова ("🔟")
  "imageUrl": string|null   // URL картинки (опционально)
}
```

### Правила заполнения

#### 1. **phonemesSet** (набор фонем)
- Включает ВСЕ уникальные фонемы из всех слов урока
- Порядок не важен
- Может содержать дополнительные фонемы для усложнения (2-3 отвлекающие)

```json
// Пример: слова TEN, NET, PEN, PET
"phonemesSet": ["t", "e", "n", "p", "s"]
// "s" - дополнительная отвлекающая буква
```

#### 2. **phonemes** в words
- Массив фонем, из которых состоит слово
- Порядок важен!
- Для диграфов (sh, ch, th) используйте один элемент

```json
// Правильно для слова "ship"
"phonemes": ["sh", "i", "p"]

// Неправильно
"phonemes": ["s", "h", "i", "p"]
```

#### 3. **emoji**
- Используйте эмодзи, связанные с переводом слова
- Для чисел: 🔟 (10), 1️⃣ (1), 2️⃣ (2)
- Для животных: 🐕 (собака), 🐈 (кошка)
- Для предметов: ☕ (чашка), 🖊️ (ручка)

#### 4. **transcription**
- Используйте IPA символы в квадратных скобках
- Примеры: [ten], [kæt], [ʃɪp]

---

## 📝 Добавление слов в существующий урок

### Шаг 1: Откройте JSON файл урока

```bash
# Например, для урока 1
data/trainer/lesson_01.json
```

### Шаг 2: Добавьте новое слово в массив words

```json
{
  "id": 1,
  "words": [
    // ... существующие слова ...
    
    // Добавьте новое слово
    {
      "word": "set",
      "phonemes": ["s", "e", "t"],
      "translation": "набор",
      "transcription": "[set]",
      "emoji": "📦",
      "imageUrl": null
    }
  ]
}
```

### Шаг 3: Обновите wordCount

```json
{
  "wordCount": 5  // Было 4, стало 5
}
```

### Шаг 4: Проверьте phonemesSet

Убедитесь, что все фонемы нового слова есть в `phonemesSet`:

```json
// Если в новом слове есть буква "s"
"phonemesSet": ["t", "e", "n", "p", "s"]  // ✅ "s" есть
```

---

## 🎨 Изменение дизайна

### Цвета

Все цвета находятся в `<style>` блоке файла `trainer/index.html`:

```css
/* Изменить основной градиент */
body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

/* Изменить цвет успеха */
.btn-primary {
    background: linear-gradient(135deg, #38ef7d, #11998e);
}

/* Изменить цвет ошибки */
.shake {
    /* Добавьте красный border */
    border: 2px solid #f45c43;
}
```

### Шрифты

```css
/* Изменить основной шрифт */
body {
    font-family: 'Comic Sans MS', 'Arial', sans-serif;
}

/* Размеры шрифтов */
.header h1 {
    font-size: 3rem;  /* Было 2.5rem */
}
```

### Анимации

```css
/* Добавить новую анимацию */
@keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
}

.bounce {
    animation: bounce 0.5s ease;
}
```

---

## ⚙️ Добавление новой механики

### Пример: Добавление звуковых эффектов

```javascript
// Найдите класс TrainerApp в index.html
class TrainerApp {
    constructor() {
        // ... существующий код ...
        
        // Добавьте звуки
        this.sounds = {
            correct: new Audio('/sounds/correct.mp3'),
            wrong: new Audio('/sounds/wrong.mp3')
        };
    }
    
    checkAnswer() {
        const word = this.currentLesson.words[this.currentWordIndex];
        const userWord = this.currentPhonemes.join('');
        const correctWord = word.phonemes.join('');

        if (userWord === correctWord) {
            this.sounds.correct.play();  // ← Добавьте звук
            this.score += 10;
            this.nextWord();
        } else {
            this.sounds.wrong.play();    // ← Добавьте звук
            const card = document.querySelector('.card');
            card.classList.add('shake');
            setTimeout(() => card.classList.remove('shake'), 500);
        }
    }
}
```

### Пример: Добавление таймера

```javascript
renderTrainer() {
    // ... существующий код ...
    
    // Добавьте таймер
    this.startTime = Date.now();
    this.timerInterval = setInterval(() => {
        const elapsed = Math.floor((Date.now() - this.startTime) / 1000);
        document.getElementById('timer').textContent = `⏱ ${elapsed}s`;
    }, 1000);
}

showResults() {
    clearInterval(this.timerInterval);  // Остановите таймер
    // ... остальной код ...
}
```

### Пример: Добавление подсказок

```javascript
renderTrainer() {
    const app = document.getElementById('app');
    app.innerHTML = `
        <div class="card">
            <!-- ... существующий контент ... -->
            
            <!-- Добавьте кнопку подсказки -->
            <button class="btn btn-secondary" onclick="app.showHint()">
                💡 Подсказка
            </button>
        </div>
    `;
}

showHint() {
    const word = this.currentLesson.words[this.currentWordIndex];
    alert(`Первая буква: ${word.phonemes[0]}`);
    this.score -= 2;  // Штраф за подсказку
}
```

---

## 🧪 Тестирование

### Ручное тестирование

**Чеклист перед публикацией:**

- [ ] Все уроки открываются без ошибок
- [ ] JSON файлы валидны (проверьте на [jsonlint.com](https://jsonlint.com/))
- [ ] Фонемы в phonemesSet совпадают с phonemes в словах
- [ ] wordCount соответствует реальному количеству слов
- [ ] Все emoji отображаются корректно
- [ ] Слова собираются правильно
- [ ] Очки начисляются корректно
- [ ] Результаты показываются правильно
- [ ] Приложение работает на мобильных устройствах

### Тестирование JSON

```bash
# Проверьте валидность JSON онлайн
https://jsonlint.com/

# Или через Python
python3 -m json.tool data/trainer/lesson_05.json
```

### Тестирование локально

```bash
# Запустите локальный сервер
python3 -m http.server 8000

# Откройте в браузере
http://localhost:8000/trainer/
```

### Проверка в консоли браузера

```javascript
// Откройте DevTools (F12) и выполните:

// Проверить загрузку урока
fetch('/data/trainer/lesson_05.json')
    .then(r => r.json())
    .then(data => console.log(data));

// Проверить валидность структуры
const lesson = {/* ваш JSON */};
const required = ['id', 'title', 'emoji', 'rule', 'description', 'phonemesSet', 'wordCount', 'words'];
required.forEach(field => {
    if (!(field in lesson)) console.error(`Missing: ${field}`);
});
```

---

## 📊 Примеры готовых уроков

### Простой урок (3 буквы)

```json
{
  "id": 5,
  "title": "Урок 5: Диграф SH",
  "emoji": "🚢",
  "rule": "Сочетание SH читается как [ʃ]",
  "description": "Изучаем звук SH",
  "phonemesSet": ["sh", "i", "p", "o", "t", "e"],
  "wordCount": 3,
  "estimatedTime": 5,
  "words": [
    {
      "word": "ship",
      "phonemes": ["sh", "i", "p"],
      "translation": "корабль",
      "transcription": "[ʃɪp]",
      "emoji": "🚢"
    },
    {
      "word": "shop",
      "phonemes": ["sh", "o", "p"],
      "translation": "магазин",
      "transcription": "[ʃɒp]",
      "emoji": "🏪"
    },
    {
      "word": "shot",
      "phonemes": ["sh", "o", "t"],
      "translation": "выстрел",
      "transcription": "[ʃɒt]",
      "emoji": "🎯"
    }
  ]
}
```

### Сложный урок (4+ буквы)

```json
{
  "id": 6,
  "title": "Урок 6: Слова из 4 букв",
  "emoji": "🐸",
  "rule": "Практика чтения слов из 4 букв",
  "description": "Собираем более длинные слова",
  "phonemesSet": ["f", "r", "o", "g", "s", "t", "p"],
  "wordCount": 2,
  "estimatedTime": 7,
  "words": [
    {
      "word": "frog",
      "phonemes": ["f", "r", "o", "g"],
      "translation": "лягушка",
      "transcription": "[frɒg]",
      "emoji": "🐸"
    },
    {
      "word": "stop",
      "phonemes": ["s", "t", "o", "p"],
      "translation": "остановка",
      "transcription": "[stɒp]",
      "emoji": "🛑"
    }
  ]
}
```

---

## 🚀 Публикация изменений

### Через Git

```bash
# 1. Добавьте файлы
git add data/trainer/lesson_05.json
git add trainer/index.html

# 2. Создайте коммит
git commit -m "Add lesson 5: Digraph SH"

# 3. Отправьте на GitHub
git push origin main

# 4. Подождите 2-3 минуты для деплоя
```

### Проверка деплоя

1. Откройте [Actions](https://github.com/andreacebotarev-svg/englishlessons/actions)
2. Дождитесь зелёной галочки ✅
3. Откройте https://eng-tutor.ru/trainer/
4. Очистите кэш (Ctrl+Shift+R)

---

## ❓ Частые вопросы

### Q: Урок не появляется в списке
**A:** Проверьте:
1. JSON файл валиден
2. Номер урока добавлен в `getAvailableLessons()`
3. GitHub Pages успешно задеплоился
4. Кэш браузера очищен

### Q: Слово не собирается правильно
**A:** Проверьте:
1. Порядок фонем в `phonemes` совпадает с написанием
2. Все фонемы из слова есть в `phonemesSet`
3. Диграфы (sh, ch) записаны одним элементом

### Q: Как изменить количество звёзд?
**A:** В функции `showResults()` измените условия:
```javascript
const stars = percentage >= 90 ? '⭐⭐⭐' : 
              percentage >= 70 ? '⭐⭐' : 
              percentage >= 50 ? '⭐' : '';
```

### Q: Можно ли добавить картинки вместо emoji?
**A:** Да, используйте поле `imageUrl`:
```json
{
  "emoji": "🐕",
  "imageUrl": "/images/dog.png"
}
```

Затем обновите код рендеринга в `renderTrainer()`.

---

## 📞 Поддержка

Если возникли вопросы:
- Email: andreacebotarev@gmail.com
- GitHub Issues: [создать issue](https://github.com/andreacebotarev-svg/englishlessons/issues)

---

✨ **Удачи в создании новых уроков!**