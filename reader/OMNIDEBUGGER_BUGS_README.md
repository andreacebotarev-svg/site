# 🐛 OmniDebugger Bug Control System

Расширенная система контроля багов для OmniDebugger, позволяющая автоматически отслеживать, регистрировать и управлять ошибками в приложении.

## 🚀 Возможности

### Автоматическая регистрация багов
- **Из ошибок JavaScript**: Перехват `window.onerror` и `unhandledrejection`
- **Из сетевых ошибок**: Автоматическое создание багов при сетевых сбоях
- **Из операций с книгами**: Регистрация багов при ошибках загрузки/парсинга книг
- **Из консольных ошибок**: Перехват `console.error` с автоматическим созданием багов

### Ручное управление багами
- **Создание багов**: `debugger.reportBug(title, description, severity, context)`
- **Обновление статуса**: Открыт → Исследуется → Чинится → Закрыт
- **Добавление комментариев**: История обсуждения бага
- **Добавление тегов**: Категоризация багов
- **Связывание событий**: Автоматическое прикрепление релевантных логов

### Статистика и аналитика
- **Общая статистика**: Количество багов, открытые, недавние
- **По статусам**: Распределение по этапам жизненного цикла
- **По severity**: Критичность багов
- **Фильтрация**: Поиск и фильтрация багов

### Хранение и экспорт
- **localStorage**: Автоматическое сохранение между сессиями
- **Экспорт в JSON**: Скачивание полного отчета о багах
- **Интеграция с логами**: Связь багов с событиями в истории

## 💻 API

### Основные методы

```javascript
// Регистрация нового бага
const bugId = debugger.reportBug(
    "Кнопка не работает",
    "При клике на кнопку сохранения ничего не происходит",
    "high",
    { component: "SaveButton", userId: 123 }
);

// Обновление статуса
debugger.updateBugStatus(bugId, "investigating", "Начинаю исследование");

// Добавление комментария
debugger.addBugComment(bugId, "developer", "Нашел причину - отсутствует обработчик");

// Добавление тегов
debugger.addBugTags(bugId, ["ui", "critical", "regression"]);

// Получение статистики
const stats = debugger.getBugStats();
// { total: 15, open: 5, recent: 3, byStatus: {...}, bySeverity: {...} }
```

### Консольные команды

```javascript
// Показать справку
bug.help()

// Создать баг
bug.report("Test Bug", "Description", "medium")

// Показать все баги
bug.list()

// Показать баги по фильтру
bug.list({ status: "open", severity: "high" })

// Обновить статус
bug.status(1, "closed", "Fixed in commit abc123")

// Показать детали бага
bug.get(1)

// Показать статистику
bug.stats()

// Экспорт багов
bug.export()
```

## 🏗️ Архитектура

### Структура бага
```javascript
{
    id: 1,
    title: "Кнопка не работает",
    description: "Подробное описание проблемы",
    severity: "high", // low, medium, high, critical
    status: "open", // open, investigating, fixing, closed
    context: { component: "SaveButton", ... },
    createdAt: 1640995200000,
    updatedAt: 1640995200000,
    events: [
        {
            type: "ERROR_OCCURRED",
            data: { error: "TypeError: ..." },
            timestamp: 1640995200000
        }
    ],
    tags: ["ui", "button"],
    comments: [
        {
            author: "developer",
            comment: "Нашел проблему",
            timestamp: 1640995200000
        }
    ]
}
```

### Автоматическое определение severity
- **Critical**: TypeError, ReferenceError, необработанные исключения
- **High**: Promise rejection, серьезные ошибки
- **Medium**: Сетевые ошибки, warnings
- **Low**: Остальные случаи

## 🧪 Тестирование

### Тестовый файл
`test-omnidebugger-bugs.html` - Полный интерфейс для тестирования системы багов

### Генерация тестовых багов
```javascript
// Генерация различных типов ошибок
generateTestError()     // JavaScript error
generateNetworkError()  // Network error
generatePromiseError()  // Promise rejection
generateCustomBug()     // Ручной баг
```

## 📊 Использование в коде

### Интеграция с существующими обработчиками
```javascript
// В catch блоках
try {
    riskyOperation();
} catch (error) {
    debugger.reportBugFromError(error, {
        component: "RiskyComponent",
        operation: "riskyOperation"
    });
}
```

### Привязка к событиям
```javascript
// При ошибке загрузки книги
debugger.logBookError(bookId, error, context);
// Автоматически создаст баг
```

## 🔄 Жизненный цикл бага

1. **Создание**: Баг регистрируется автоматически или вручную
2. **Исследование**: Статус меняется на "investigating", добавляются комментарии
3. **Исправление**: Статус "fixing", привязываются коммиты/исправления
4. **Закрытие**: Баг помечается как "closed" с комментарием

## 📈 Метрики и аналитика

### Доступные метрики
- Общее количество багов
- Количество открытых багов
- Недавние баги (последние 7 дней)
- Распределение по статусам
- Распределение по severity
- Количество событий на баг
- Количество комментариев

### Экспорт данных
- JSON файл со всеми багами
- Включает полную историю событий и комментариев
- Совместим с системами управления задачами

## 🚀 Будущие улучшения

- **Интеграция с внешними системами**: Jira, GitHub Issues
- **Уведомления**: Email/webhook уведомления о новых багах
- **Аналитика**: Графики и тренды по багам
- **Приоритизация**: Автоматический расчет приоритета
- **Тэги и категории**: Более гибкая система категоризации
