# 📔 EngTutor Ecosystem: Comprehensive Documentation

> **Status**: Production / Modular  
> **Target**: Internal Developers & AI Agents  
> **Last Updated**: February 6, 2026

---

## 🏗️ Архитектурный обзор (High-Level)

Проект представляет собой **многомодульную образовательную платформу**, построенную по принципу «Hub & Spokes».

- **Центральный узел (Hub)**: `index.html` (Лендинг-визитка).
- **Спутники (Spokes)**: Независимые специализированные веб-приложения (`reader`, `aihelper`, `trainers`), интегрированные через IFrame или прямые ссылки.

---

## 📂 Структура системы

### 1. 🌐 Landing Page (Entry Point)

**Путь**: `/` (Корень)
Основная страница захвата внимания и навигации.

- **Технологии**: Vanilla HTML5/CSS3/JS.
- **Ключевой функционал**:
  - **Glassmorphism UI**: Использование прозрачных карточек и блюра.
  - **Platform Preview**: Модальная система предпросмотра модулей (`platform-modal.js`).
  - **Reveal Animations**: Плавное появление контента при скролле.

### 2. 📖 Smart Reader v5

**Путь**: `/reader/`
Самый сложный технический модуль для интерактивного чтения.

- **Возможности**:
  - Поддержка форматов: EPUB, FB2, TXT.
  - **Interactivity**: Клик по любому слову вызывает перевод и озвучку.
  - **PWA**: Возможность установки на телефон и работа оффлайн.
- **Архитектура**: Пагинация на базе JS-воркеров, хранение книг в IndexedDB.

### 3. 🤖 AI Helper Layer

**Путь**: `/aihelper/`
Интеллектуальный помощник для учеников и родителей.

- **Инструменты**:
  - `ai-tutor.js`: Ассистент по учебным вопросам.
  - `ai-sales.js`: Помощник по навигации и продажам.
- **Безопасность**: Клиентское шифрование запросов к OpenRouter через `crypto.js`.

### 4. 🎮 English Trainers (Gamification)

**Путь**: `/english-trainers/` & `/trainer/`
Набор микро-игр для отработки навыков.

- **Типы**: Arcade (Much/Many), Master (Tenses), Basics (Present Simple).
- **Движок**: `trainer-core.js` — абстрактная база для создания новых упражнений через JSON-генераторы.

### 5. 🎓 Lesson Engine

**Путь**: `/dist/`
Система интерактивных уроков, созданных по методике TBLT (Task-Based Language Learning).

- **Особенности**:
  - Интегрированные Flashcards.
  - Kanban-доски для слов (To Learn -> Known).
  - **Data-Driven**: Каждый урок (`.html`) подгружает свои данные из `/data/*.json`.

---

## ⚙️ Управление данными

- **Content**: Все тексты уроков и вопросы тренажеров вынесены в JSON.
- **Assets**:
  - `/assets/`: Общие стили и скрипты лендинга.
  - `/images/`: Ресурсы, сгруппированные по ID уроков.
- **Legacy Control**: В папке `dist/` находятся скомпилированные версии старых уроков.

---

## 🧐 Технический аудит и риски

1.  **Децентрализация**: Отсутствие единой системы сборки (Vite/Webpack) в корне проекта. Каждый модуль настраивается отдельно.
2.  **Дублирование кода**: Файлы вроде `crypto.js` и `state-manager.js` встречаются в разных папках с минимальными отличиями.
3.  **Безопасность**: Клиентская реализация шифрования API (CryptoJS) требует осторожности — ключи технически доступны в рантайме.
4.  **Персистентность**: Данные о прогрессе в разных модулях хранятся в разных инстансах `LocalStorage`, что затрудняет создание единого профиля ученика.

---

## 🚀 Рекомендации по развитию

1.  **Миграция на PocketBase**: Использовать наработанную базу в `/engplatform` для синхронизации всех модулей в единую БД.
2.  **CDN активов**: Вынести общие библиотеки в единый `assets/shared/`.
3.  **TypeScript**: Постепенный переход на TS (уже начат в `engplatform`) для всех критических модулей синхронизации.
